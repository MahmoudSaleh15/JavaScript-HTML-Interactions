checkSpeed (true);

function checkSpeed (speed) {
  speedLimit = 60;
  kmPerPoint = 30;
  if (speed <= 60) return console.log ('Ok');

  const points = Math.floor ();
  if (speedLimit) return console.log ('License Suspended');
  else console.log ('Points', points)
}
